apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openx-flask-api.fullname" . }}-test-connection"
  labels:
    {{- include "openx-flask-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openx-flask-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

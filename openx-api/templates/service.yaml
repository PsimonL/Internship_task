apiVersion: v1
kind: Service
metadata:
  name: {{ include "openx-api.fullname" . }}
  labels:
    {{- include "openx-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "openx-api.selectorLabels" . | nindent 4 }}


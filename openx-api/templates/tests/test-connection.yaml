apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openx-api.fullname" . }}-test-connection"
  labels:
    {{- include "openx-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openx-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
